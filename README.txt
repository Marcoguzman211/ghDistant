Pour mettre en place le pre-commit hook:
1. Faites une copie du fichier `pre-commit` qui est dans `hooks/pre-commit` dans `.git/hooks/` 
2. Assurez vous que ce fichier est éxécutable : 
 chmod +x .git/hooks/pre-commit